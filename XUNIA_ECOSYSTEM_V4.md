# XUNIA / XuniaDAO // Ecosystem v4

XuniaDAO is the canonical **XUNIAverse domain ontology and registry root**. The Black House / GPT-DOUG-LLM MAX remains the ecosystem-wide global control plane.

```text
THE BLACK HOUSE / GPT-DOUG-LLM MAX
             ↓
     XUNIA / XuniaDAO
 domain ontology • registry • identity graph
 CRM • compliance • licensing • governance
             ↓
        ZYRA EXECUTION GATE
             ↓
RED HOUSE | GREEN HOUSE | ORANGE HOUSE | GLASS ONION
             ↓
        EVIDENCE RETURN
```

This keeps global control-plane authority and XUNIAverse domain authority separate instead of treating every repository as an equal root.

The ecosystem collectively declares all six conceptual AI layers: Classical AI, Machine Learning, Neural Networks, Deep Learning, Generative AI and Agentic AI. XuniaDAO provides domain structure and registry context; it does not need to independently implement every model family.

Canonical ecosystem registry: `sonoxo/gpt-doug-llm/ecosystem/registry.v4.json`.

**Boundary:** XUNIAverse membership does not rewrite upstream ownership, create vendor affiliation, grant third-party credentials, or authorize access to external systems.
